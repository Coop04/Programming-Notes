#include <stdio.h>
int main()
{
    int a,b,diff;
    printf("Enter two numbers to find their difference ");
    scanf("%d %d",&a,&b);

    diff=a-b;

    printf("Difference is %d",diff);
    return 0;
}