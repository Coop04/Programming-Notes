For pandas environment to work 
1. py -m venv env
2. activate the environment (.\env\Scripts.activate)
3. now change the python interpritter (ctrl+shift+p -> select env\Scripts.python.exe)
4. change the environment to the newly created environmentfrom the jupyter navbar at the top