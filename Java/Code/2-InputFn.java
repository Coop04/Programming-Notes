import java.util.Scanner;
class InputFn {
    public static void main(String args[]) {
        Scanner sc=new Scanner(System.in);
        int a=sc.nextInt();//reads an integer.
        double b=sc.nextDouble();// reads a double (floating-point number).
        char c=sc.next().charAt(0);//reads a character by first reading a string. 
        String d=sc.nextLine();//reads a string.
        String e=sc.next();// reads a string until the next whitespace character.
        int a[]=new int[10];
    }
}